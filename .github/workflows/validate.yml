name: validate

on:
  push:
    branches: [main]
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Structural consistency (context vs JSON Schema, SHA256SUMS coverage)
        run: python3 scripts/validate.py

      - name: Published digests match SHA256SUMS
        run: sha256sum -c SHA256SUMS

      - name: Round-trip a sample credential through both documents
        run: |
          npm install --no-save jsonld@8 ajv@8 ajv-formats@3
          node scripts/roundtrip.mjs
